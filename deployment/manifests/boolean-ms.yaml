apiVersion: apps/v1
kind: Deployment
metadata:
  name: boolean-ms
  namespace: prpo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: boolean-ms
  template:
    metadata:
      labels:
        app: boolean-ms
    spec:
      containers:
      - name: boolean-ms
        image: adrian4096/prpo-app-boolean:${TAG}
        ports:
        - containerPort: 8000
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: boolean-ms
  namespace: prpo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: boolean-ms
