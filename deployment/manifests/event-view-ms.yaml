apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-view-ms
  namespace: prpo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-view-ms
  template:
    metadata:
      labels:
        app: event-view-ms
    spec:
      containers:
      - name: event-view-ms
        image: adrian4096/prpo-app-event-view:${TAG}
        ports:
        - containerPort: 8000
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: event-view-ms
  namespace: prpo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: event-view-ms
