apiVersion: apps/v1
kind: Deployment
metadata:
  name: ical-ms
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ical-ms
  template:
    metadata:
      labels:
        app: ical-ms
    spec:
      containers:
      - name: ical-ms
        image: adrian4096/prpo-app-ical:${TAG}
        ports:
        - containerPort: 8000
        livenessProbe:
          httpGet:
            path: /podatki/health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /podatki/health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 10
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: ical-ms
spec:
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: ical-ms
